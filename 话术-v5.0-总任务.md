# 话术 v5.0 — 总任务（基于4.0基线）

> 唤醒词: 话术
> 项目: MysticDao Frontend v5.0
> 基线: v4.0（agent-4-ai-experience + agent-4-daily-social + agent-4-naming 已merge）
> 仓库: github.com/miemiege/mysticdao-frontend
> 用户名: miemiege
> Token: <YOUR_GITHUB_TOKEN>
> 工作分支: v5.0-rebuild（从4.0基线checkout）
> 技术栈: React 19 + Vite + Tailwind CSS + TypeScript + HashRouter
> 部署: GitHub Pages
> 融合负责人: CLI Agent

---

## 4.0 基线确认（已有功能，不要动）

| 模块 | 已有组件 |
|:---|:---|
| **Daily** | FortuneCard, GlobalCounter, LuckyInfo, RitualDrawing, RollingNumber, ScoreRing, SharePoster |
| **Bazi** | BirthForm, FourPillars, PillarColumn, ReadingResult, ChineseNaming, ForeignerNaming, calendar, data |
| **Fengshui** | BaguaMap, Compass, Compass3D, DirectionPanel, fengshuiData |
| **FengshuiCamera** | BaguaScene3D, FengshuiCamera, useCaptureFreeze, useCompassRotation, useFloatAnimation, BaguaCompass, ParticleRing, TaijiSymbol |
| **Compass** | WebCompass, compassData |
| **通用** | BaguaLoader, BreathingTypewriter, Footer, Layout, Navbar, SacredIcons, TaijiLoader, TypewriterText |
| **UI** | shadcn/ui 全套组件 |
| **Hooks** | use-mobile, useCamera, useCompass, useCompassPC, useGeolocation, usePermission, usePreload |
| **数据** | gua64, bagua, yijing, bazi/*, fengshui/*, namingChars, wuxing, tiangan, dizhi, nayin, shishen |
| **页面** | Home, Daily, Bazi, FengShui, CompassPage, UserProfile, Pricing |

---

## 5.0 升级总览（需要开发的全部功能）

### 模块1: 符咒引擎 + AI图片 — 视觉核心
**负责人**: Agent-5A
**分支**: agent-5a-talisman

```
新建:
- src/components/talisman/TalismanSVG.tsx      # SVG符咒生成器
- src/components/talisman/TalismanRenderer.tsx # AI图层+SVG fallback渲染器
- src/lib/theme.ts                             # 五行主题配色
- src/lib/ai-prompts.ts                        # Pollinations.AI提示词
- src/lib/cardRarity.ts                        # 卡牌稀有度

修改:
- src/pages/Daily.tsx                          # 插入TalismanRenderer到结果页
- src/lib/storage.ts                           # 添加AI缓存逻辑
```

**功能要求**:
1. TalismanSVG:
   - 3种符头: SanqingHead(三清), SantaiHead(三台), ChilingHead(敕令)
   - 符胆(FuGall): 根据五行显示不同字符(金=罡, 木=化, 水=井, 火=马, 土=井)
   - 印章(SealStamp): 6等级(上上签/上吉/中吉/小吉/平/需谨慎)
   - 六爻线(HexagramLines): 带发光效果
   - 背景纹理(BackgroundPattern): SVG噪点模拟"数字墨玉"

2. TalismanRenderer:
   - 三层渲染: AI图片(顶部) + SVG(中间) + 黑色背景(底部)
   - AI图片来源: Pollinations.AI(免费, 无API key)
   - 加载: localStorage缓存(7天过期, 20张上限), 8秒超时
   - crossOrigin="anonymous", SVG fallback覆盖失败

3. 主题系统(theme.ts):
   - 5套配色: 金(Gold #C8A45C)/木(Teal #2D6B5F)/水(Blue #1E5A8C)/火(Crimson #8B2C2C)/土(Amber #B8860B)
   - getTheme(element): 返回{primary, secondary, accent, bg, text, seal, cloud}
   - getSealInfo(score): 6等级映射

4. AI提示词(ai-prompts.ts):
   - getPollinationsUrl(): 构造Pollinations.AI图片URL
   - getAICacheKey(): 确定性缓存键

---

### 模块2: 六爻绘制 + 首页粒子 — 动画系统
**负责人**: Agent-5B
**分支**: agent-5b-animations

```
新建:
- src/components/daily/HexagramDraw.tsx        # 六爻SVG逐线绘制
- src/components/home/TaijiParticles.tsx       # 太极粒子背景

修改:
- src/pages/Home.tsx                           # 替换旧粒子为TaijiParticles
- src/pages/Daily.tsx                          # 插入HexagramDraw到结果页
```

**功能要求**:
1. HexagramDraw:
   - 接收 Yao[]: { value: 0|1, changing: boolean }
   - SVG stroke-dashoffset动画: 每爻从上到下依次绘制
   - strokeColor可配置(默认#c8a45c)
   - onComplete回调
   - 绘制完成后显示阴阳/动爻标签

2. TaijiParticles:
   - Canvas 2D, requestAnimationFrame
   - 双色粒子: 金(#C8A45C)和红(#9B2C2C)
   - 轨道旋转 + 脉动发光
   - 80粒子(hero) / 40粒子(footer)
   - 替换Home页原有粒子系统

---

### 模块3: 多平台分享卡 — 社交传播
**负责人**: Agent-5C
**分支**: agent-5c-share

```
新建:
- src/components/share/ShareCard.tsx           # 多平台分享卡
- src/lib/share-platforms.ts                   # 平台配置

修改:
- src/pages/Daily.tsx                          # 添加ShareCard modal
```

**功能要求**:
1. ShareCard:
   - 6平台: Web/INS Feed/INS Story/Twitter/WeChat/Universal
   - html2canvas导出PNG
   - 平台切换UI
   - 不同平台不同尺寸比例
   - 包含: 符咒、卦象信息、金色引语、分数、印章、品牌

2. share-platforms.ts:
   - PlatformKey类型
   - PLATFORM_CONFIGS: 各平台尺寸/颜色/图标配置

---

### 模块4: 用户系统 — 卡册/运势PK/留言板/签到
**负责人**: Agent-5D
**分支**: agent-5d-social

```
新建:
- src/components/daily/CardAlbum.tsx           # 卡册
- src/components/daily/CardFlip.tsx            # 卡片翻转
- src/components/daily/CardShare.tsx           # 卡片分享
- src/components/daily/FortunePK.tsx           # 运势PK
- src/components/daily/AnonymousBoard.tsx      # 匿名留言板
- src/hooks/useStreak.ts                       # 连续签到

修改:
- src/pages/UserProfile.tsx                    # 集成卡册/签到
- src/lib/storage.ts                           # 添加卡册/签到存储
```

**功能要求**:
1. CardAlbum:
   - 展示收藏的卦象卡片
   - 按稀有度分类
   - 卡片详情页

2. CardFlip:
   - 3D翻转效果
   - CSS transform perspective

3. FortunePK:
   - 双人运势对比
   - 分数对比可视化

4. AnonymousBoard:
   - 匿名发布留言
   - 点赞/回复

5. useStreak:
   - 每日签到检测
   - 连续天数计算
   - 断签重置

6. cardRarity:
   - 5等级: Common/Rare/Epic/Legendary/Mythic
   - 根据卦象+分数计算稀有度

---

### 模块5: Daily结果页重写 — 终极视觉
**负责人**: Agent-5E
**分支**: agent-5e-daily-result

```
修改:
- src/pages/Daily.tsx                          # 完全重写result部分
```

**功能要求**:
1. 卷轴容器:
   - 上下木质轴头装饰
   - scaleY 0→1展开动画
   - 深褐色到黑色径向渐变背景

2. 符咒大图:
   - TalismanRenderer居中
   - 卦象颜色径向光晕
   - 缓慢浮动动画

3. 卦名书法:
   - Noto Serif SC字体
   - 金色光晕text-shadow
   - 模糊到清晰入场

4. 分数+印章:
   - RollingNumber滚动
   - 印章弹簧动画盖印
   - 等级: 上/吉/中/平

5. 六爻:
   - HexagramDraw SVG动画
   - 阴阳/动爻标签

6. 运势维度:
   - 4个ScoreRing
   - 依次延迟入场

7. AI解读:
   - BreathingTypewriter
   - 可折叠段落
   - 追问按钮(朱砂红)

8. 操作按钮:
   - 收藏/分享/再来一卦
   - 底部悬浮面板(移动端)

**必须保留**: 所有现有handler, ShareCard, SharePoster, 追问系统

---

## 环境准备

```bash
cd /tmp/mysticdao-frontend
git checkout v5.0-rebuild          # 4.0基线分支
npm install                        # 如未安装
npm run build                      # 验证基线可build
```

## 依赖安装（各Agent按需）

```bash
# 符咒引擎不需要额外依赖（纯SVG）
# 分享卡需要:
npm install html2canvas

# 其他模块使用现有依赖
```

## 环境铁律

1. npm install 超过60秒 → 重试
2. 每次修改后 `npm run build`，0 TypeScript错误才能push
3. 4.0基线已验证可build，失败是你修改导致的
4. 不要修改4.0已有hooks（useCamera/useCompass等）
5. 不要修改Bazi/Fengshui核心逻辑
6. 所有新组件用TypeScript + Tailwind + Framer Motion
7. 路由用HashRouter

## 验收标准（每个Agent）

- `npm run build` 通过，0错误
- 新功能在对应页面正常工作
- 移动端适配
- 不破坏4.0已有功能

## 推送

```bash
git add -A
git commit -m "feat(v5.0): xxx"
git remote set-url origin https://miemiege:<TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin <分支名>
```

## 融合顺序（给CLI Agent）

1. agent-5b-animations（HexagramDraw + TaijiParticles，基础动画）
2. agent-5a-talisman（符咒引擎，依赖HexagramDraw）
3. agent-5c-share（分享卡，依赖符咒）
4. agent-5d-social（用户系统，独立）
5. agent-5e-daily-result（Daily重写，依赖以上所有）

每次merge后: `npm run build` → 部署 → 验证

---

*话术 v5.0 总任务 — 基于4.0基线 — 2026-04-21*
