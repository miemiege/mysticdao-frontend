# 话术 v5.0 — 5个Agent完整任务（覆盖全部5.0功能）

> 唤醒词: 话术
> 项目: MysticDao Frontend v5.0
> 基线分支: v5.0-rebuild
> 仓库: github.com/miemiege/mysticdao-frontend
> 用户名: miemiege
> Token: <YOUR_GITHUB_TOKEN>
> 融合负责人: CLI Agent

---

## 任务 1：符咒引擎 + 六爻 + 太极粒子 — 视觉系统完善

【项目背景】
MysticDao 是东方玄学 AI 产品（React 19 + Vite + Tailwind + TypeScript），黑金配色 #000000 / #c8a45c。v5.0 需要让符咒、六爻、太极粒子这三个核心视觉组件达到产品级质量。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5a-visual

【环境准备】
cd /tmp/mysticdao-frontend
git pull origin v5.0-rebuild
git checkout -b agent-5a-visual

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || (rm -rf node_modules package-lock.json && npm install)

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → Ctrl+C → rm -rf node_modules → npm install
2. 每次修改后 npm run build，0 TypeScript 错误才能 git add
3. 基线代码已验证可 build
4. 不要修改 hooks（useCamera.ts、useCompass.ts）

【Mira · 产品设计要求】
"视觉是产品的灵魂。符咒要有'神圣感'，六爻要有'仪式感'，粒子要有'宇宙感'。"

1. TalismanSVG 增强（P0）
   • 文件: src/components/talisman/TalismanSVG.tsx（修改）
   • 符头动画: 入场时符头线条从0%到100% stroke-dashoffset绘制
   • 符胆光晕: FuGall字符周围添加脉动光晕（CSS animation，3s周期）
   • 印章动画: SealStamp盖印时添加震动效果（translateX ±2px，0.1s）
   • 背景纹理增强: 添加SVG滤镜feTurbulence，模拟宣纸质感
   • 六爻线发光: HexagramLines添加filter: drop-shadow(0 0 4px color)

2. TalismanRenderer 优化（P0）
   • 文件: src/components/talisman/TalismanRenderer.tsx（修改）
   • AI图片加载: 添加渐进模糊效果（从blur-2xl到清晰，1.5s过渡）
   • 加载骨架屏: AI图片加载前显示符咒轮廓shimmer动画
   • 错误处理: AI图片加载失败时，自动放大SVG到100%（当前是15%透明度）
   • 交叉观察: 使用IntersectionObserver，符咒进入视口时才加载AI图片

3. HexagramDraw 增强（P0）
   • 文件: src/components/daily/HexagramDraw.tsx（修改）
   • 声音同步: 每爻绘制时触发微振动（navigator.vibrate 50ms）
   • 动爻特效: changing爻绘制后添加金色粒子爆发（CSS animation，8-12个粒子）
   • 绘制音效: 配合 useRitualSound hook，播放爻线绘制音

4. TaijiParticles 优化（P1）
   • 文件: src/components/home/TaijiParticles.tsx（修改）
   • 鼠标交互: 鼠标移动时粒子被吸引/排斥
   • 触摸支持: 移动端触摸时粒子向触摸点聚集
   • 性能: 使用 OffscreenCanvas（如浏览器支持）
   • 降级: 低性能设备自动减少粒子数量（检测navigator.hardwareConcurrency）

【代码铁律】
1. 只修改已有视觉组件，不新增业务逻辑
2. 所有动画用 CSS animation 或 Framer Motion
3. 性能优先: 不要造成布局抖动
4. 移动端必须流畅

【验收标准】
- npm run build 通过
- 符咒入场有绘制动画
- 六爻绘制有粒子特效
- 太极粒子响应鼠标/触摸
- 移动端60fps

【额外要求】
- package.json version "5.0.0"
- 提交: `feat(visual): talisman glow + hexagram particles + taiji interaction`

【推送】
git add -A && git commit -m "feat(visual): talisman glow + hexagram particles + taiji interaction" && git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git && git push -u origin agent-5a-visual

---

## 任务 2：Daily结果页 + 分享系统 — 体验优化

【项目背景】
MysticDao Daily结果页已重写为卷轴天启设计，分享卡已基础实现。需要进一步优化用户体验，让结果页成为产品的"尖叫点"。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5b-daily-share

【环境准备】
cd /tmp/mysticdao-frontend && git pull origin v5.0-rebuild && git checkout -b agent-5b-daily-share

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || (rm -rf node_modules package-lock.json && npm install)

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 不要修改 hooks/相机/罗盘

【Mira · 产品设计要求】
"结果页是用户唯一会截图分享的地方。必须完美。"

1. Daily结果页微交互（P0）
   • 文件: src/pages/Daily.tsx（修改）
   • 卷轴展开: 添加音效（配合 useRitualSound）
   • 符咒浮动: 鼠标悬停时浮动暂停，显示卦象tooltip
   • 分数揭示: 滚动到分数区域时自动触发RollingNumber
   • 印章盖印: 添加"咚"的音效 + 屏幕轻微震动
   • 追问按钮: hover时放大1.05倍 + 金色光晕
   • 底部悬浮栏（移动端）: 收藏/分享/再来一卦固定在底部

2. ShareCard 完善（P0）
   • 文件: src/components/share/ShareCard.tsx（修改）
   • 导出质量: 添加4K导出选项（scale: 3）
   • 加载状态: 导出时显示进度条
   • 预览优化: 预览卡片添加符咒SVG缩略图
   • 平台适配: Twitter卡片添加hashtag自动填充
   • 微信分享: 生成二维码（使用qrcode库或API）
   • 复制链接: 自动生成带utm参数的分享链接

3. 分享海报升级（P1）
   • 文件: src/components/daily/SharePoster.tsx（修改）
   • 添加符咒元素到海报中
   • 添加用户连续天数（如果有）
   • 添加稀有度标签

4. 追问系统优化（P1）
   • 文件: src/pages/Daily.tsx
   • 追问按钮添加图标（💼/💕/⚠️）
   • 追问结果添加"再次追问"限制提示
   • 追问文本打字完成后自动滚动到底部

【代码铁律】
1. 不修改状态机逻辑
2. 不修改数据获取流程
3. 所有交互用 Framer Motion

【验收标准】
- npm run build 通过
- 所有微交互动画流畅
- ShareCard导出成功
- 移动端底部悬浮栏正常

【额外要求】
- 提交: `feat(daily): result micro-interactions + share card polish`

【推送】
git add -A && git commit -m "feat(daily): result micro-interactions + share card polish" && git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git && git push -u origin agent-5b-daily-share

---

## 任务 3：用户社交系统 — 卡册 + 运势PK + 留言板

【项目背景】
MysticDao v5.0 核心视觉已完成。需要用户社交系统让用户有留存动力和传播欲望。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5c-social

【环境准备】
cd /tmp/mysticdao-frontend && git pull origin v5.0-rebuild && git checkout -b agent-5c-social

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || (rm -rf node_modules package-lock.json && npm install)

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 基线代码已验证可 build
4. 不要修改已有组件源码

【Mira · 产品设计要求】
"用户抽到好卦后，第一个冲动是收藏，第二个是给朋友看，第三个是看别人的。"

1. 卡册系统 CardAlbum（P0）
   • 文件: src/components/daily/CardAlbum.tsx（新建）
   • 文件: src/components/daily/CardFlip.tsx（新建）
   • 数据: 复用 src/lib/storage.ts 已有 addFavorite/isFavorite/getFavorites
   • 网格布局: 3列（桌面）/ 2列（平板）/ 1列（手机）
   • 每张卡片: 符咒缩略图 + 卦名 + 分数 + 稀有度颜色边框 + 日期
   • CardFlip: CSS perspective + rotateY 180deg，0.6s动画
   • 背面: 详细信息（祈福主题、AI解读摘要、删除按钮）
   • 筛选: 按稀有度/日期/五行筛选
   • 空状态: "还没有收藏的卦象，去抽一张吧"

2. 运势PK FortunePK（P0）
   • 文件: src/components/daily/FortunePK.tsx（新建）
   • 界面:
     - 左侧: 当前用户今日卦象（自动填充）
     - 右侧: 输入对方卦象名或从卡册选择
     - 对比维度: 总分、事业、感情、财富、健康（5个进度条）
     - 结果文案: "你的事业运更强" / "感情运相当" 等
     - 分享按钮: 生成PK结果图
   • 动画: 分数对比时进度条弹跳入场

3. 匿名留言板 AnonymousBoard（P1）
   • 文件: src/components/daily/AnonymousBoard.tsx（新建）
   • 数据: localStorage，key `mysticdao_board`
   • 功能:
     - 发布感悟（140字限制，显示字数）
     - 展示最近30条
     - 点赞（localStorage记录已点赞）
     - 每条显示: 卦象名、分数、内容、时间、点赞数
   • 界面: 卡片列表，金色边框分隔
   • 空状态: "还没有留言，做第一个分享的人吧"

4. UserProfile 集成（P1）
   • 文件: src/pages/UserProfile.tsx（修改）
   • 添加3个Tab: 卡册 / 运势PK / 留言板
   • 顶部显示用户统计: 总抽卦次数、收藏数、最高分数

【代码铁律】
1. 卡册数据复用 storage.ts，不新增存储键
2. 运势PK纯前端，不依赖后端
3. 留言板数据localStorage存储
4. 所有新组件 TypeScript + Tailwind + Framer Motion

【验收标准】
- npm run build 通过
- 卡册显示正确
- CardFlip 3D翻转流畅
- 运势PK对比正确
- 留言板可发布、点赞

【额外要求】
- 提交: `feat(social): card album + fortune PK + anonymous board`

【推送】
git add -A && git commit -m "feat(social): card album + fortune PK + anonymous board" && git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git && git push -u origin agent-5c-social

---

## 任务 4：连续签到系统 — 留存激励

【项目背景】
MysticDao v5.0 需要签到系统培养用户每日使用习惯。签到与卡册系统联动，连续签到可获得稀有度加成。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5d-streak

【环境准备】
cd /tmp/mysticdao-frontend && git pull origin v5.0-rebuild && git checkout -b agent-5d-streak

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || (rm -rf node_modules package-lock.json && npm install)

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误

【Mira · 产品设计要求】
"连续签到7天，第7天的卦象稀有度+1级。这是让用户每天回来的理由。"

1. useStreak Hook（P0）
   • 文件: src/hooks/useStreak.ts（新建）
   • 接口:
     ```typescript
     interface StreakData {
       lastCheckIn: string;     // YYYY-MM-DD
       currentStreak: number;   // 当前连续天数
       longestStreak: number;   // 历史最高
       totalCheckIns: number;   // 总签到次数
       history: string[];       // 最近30天签到记录
     }
     ```
   • 方法:
     - `checkIn(): boolean` — 今日签到，已签到返回false
     - `getStreakData(): StreakData`
     - `canCheckIn(): boolean`
   • 逻辑:
     - 断签检测: 上次签到不是昨天 → currentStreak 重置为1
     - 同一天不重复签到
     - 数据存 localStorage，key `mysticdao_streak`

2. StreakCalendar 组件（P0）
   • 文件: src/components/daily/StreakCalendar.tsx（新建）
   • 顶部: 连续天数大数字（"7天"）+ 火焰图标
   • 进度条: 7天一个周期，当前进度高亮
   • 日历网格: 最近30天，已签到 = 金色实心圆，未签到 = 虚线圆，今日 = 脉动边框
   • 里程碑: 3天/7天/14天/30天标记，达成时有特殊图标
   • 签到按钮: 大圆形按钮，今日未签到时显示"签到"，已签到显示"已签到"（禁用）
   • 签到动画: 点击后数字弹跳 + 金色粒子爆发 + 屏幕震动（vibrate）

3. 稀有度加成（P1）
   • 文件: src/lib/cardRarity.ts（修改）
   • 在 getRarity 中添加 streakDays 参数
   • streak ≥ 3: Rare概率+5%
   • streak ≥ 7: Epic+5%, Legendary+3%
   • streak ≥ 14: Mythic+2%
   • 文件: src/pages/Daily.tsx（修改 generateFortune）
     - 引入 useStreak
     - 抽卦时传入当前连续天数

4. UserProfile 集成（P1）
   • 文件: src/pages/UserProfile.tsx（修改）
   • 顶部显示连续天数徽章
   • 添加签到日历Tab

【代码铁律】
1. 日期比较用 YYYY-MM-DD 字符串
2. 签到数据key: `mysticdao_streak`
3. 不修改已有业务逻辑

【验收标准】
- npm run build 通过
- 首次签到 currentStreak=1
- 连续签到递增
- 断签后重置为1
- 日历正确显示30天
- 签到动画流畅

【额外要求】
- 提交: `feat(streak): check-in system + streak calendar + rarity bonus`

【推送】
git add -A && git commit -m "feat(streak): check-in system + streak calendar + rarity bonus" && git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git && git push -u origin agent-5d-streak

---

## 任务 5：性能优化 — 代码分割 + Bundle瘦身

【项目背景】
MysticDao v5.0 当前 bundle 549KB gzip，超过500KB限额。JS chunk 1959KB minified。必须做代码分割降低首屏加载。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5e-performance

【环境准备】
cd /tmp/mysticdao-frontend && git pull origin v5.0-rebuild && git checkout -b agent-5e-performance

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || (rm -rf node_modules package-lock.json && npm install)
npm install -D rollup-plugin-visualizer vite-plugin-compression

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 只改构建配置和加载方式，不改业务逻辑

【Mira · 产品设计要求】
"首屏3秒打不开，用户直接关闭。性能是体验的第一道门槛。"

1. 代码分割 manualChunks（P0）
   • 文件: vite.config.ts（修改）
   • 配置:
     ```typescript
     build: {
       rollupOptions: {
         output: {
           manualChunks: {
             vendor: ['react', 'react-dom', 'react-router-dom'],
             motion: ['framer-motion'],
             three: ['three', '@react-three/fiber', '@react-three/drei'],
             ui: ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu', '@radix-ui/react-tabs', '@radix-ui/react-toast'],
             utils: ['html2canvas', 'lucide-react'],
           }
         }
       }
     }
     ```
   • 目标: 首屏chunk < 200KB gzip

2. 路由懒加载（P0）
   • 文件: src/App.tsx（修改）
   • 非首屏页面懒加载:
     ```typescript
     const Bazi = lazy(() => import('./pages/Bazi'));
     const FengShui = lazy(() => import('./pages/FengShui'));
     const CompassPage = lazy(() => import('./pages/CompassPage'));
     const UserProfile = lazy(() => import('./pages/UserProfile'));
     const Pricing = lazy(() => import('./pages/Pricing'));
     ```
   • Home 和 Daily 同步加载
   • Suspense fallback: src/components/TaijiLoader.tsx（复用）
   • 添加 loading 状态指示器

3. 动态导入优化（P0）
   • 文件: src/components/share/ShareCard.tsx（修改）
   • html2canvas 已经是动态导入，确保正确
   • 文件: src/components/talisman/TalismanRenderer.tsx（修改）
   • 使用 IntersectionObserver，符咒进入视口才加载AI图片

4. 压缩优化（P1）
   • 文件: vite.config.ts
   • 添加 vite-plugin-compression:
     ```typescript
     compression({
       verbose: true,
       disable: false,
       threshold: 1024,
       algorithm: 'gzip',
       ext: '.gz',
     })
     ```
   • 确保 dist/ 有 .gz 文件

5. Bundle分析（P1）
   • 文件: vite.config.ts
   • 添加 rollup-plugin-visualizer
   • 输出 dist/stats.html

6. 资源优化（P1）
   • 文件: index.html
   • 添加字体预加载: `<link rel="preload" href="..." as="font" crossorigin>`
   • 所有 img 添加 loading="lazy"
   • 检查重复依赖

【代码铁律】
1. 只改构建配置和路由加载方式
2. 不修改组件内部逻辑
3. HashRouter 模式下懒加载必须正常

【验收标准】
- npm run build 通过
- 首屏chunk < 200KB gzip
- 总bundle < 400KB gzip
- Lighthouse Performance > 70
- dist/ 中有 .gz 文件
- 路由切换有loading状态

【额外要求】
- 提交: `feat(perf): code splitting + lazy load + compression + bundle analyze`

【推送】
git add -A && git commit -m "feat(perf): code splitting + lazy load + compression + bundle analyze" && git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git && git push -u origin agent-5e-performance

---

## 融合规则（给CLI Agent）

1. 5个Agent输出代码后，先checkout到临时分支测试
2. Merge顺序:
   - agent-5e-performance（性能基础，先拆包）
   - agent-5a-visual（视觉优化，依赖性能基础）
   - agent-5b-daily-share（Daily体验，依赖视觉组件）
   - agent-5d-streak（签到系统，独立）
   - agent-5c-social（社交系统，最后，改动最大）
3. 每次merge后: `npx tsc --noEmit` → `npx vite build` → 检查bundle size → 部署
4. 冲突解决: 性能 > 功能逻辑 > 视觉效果 > 代码风格
5. 全部merge后打tag: `git tag v5.0.0 && git push origin v5.0.0`
6. 最终部署验证: Home / Daily / Bazi / UserProfile 全部页面正常

---

*话术 v5.0 — 5个Agent完整任务 — 覆盖全部5.0功能 — 2026-04-21*
