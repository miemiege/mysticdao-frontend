# 话术 v5.0 — KIMI网页版 Agent 集群任务模板

> 唤醒词: 话术
> 项目: MysticDao Frontend v5.0
> 仓库: github.com/miemiege/mysticdao-frontend
> 用户名: miemiege
> Token: <YOUR_GITHUB_TOKEN>
> 基线分支: v3.1-agent-rewrite
> 融合负责人: CLI Agent（负责merge → build → deploy）

---

## 任务 1：API层完善 → 真实数据 + 错误处理

【项目背景】
MysticDao 是东方玄学 AI 产品（React 19 + Vite + Tailwind + TypeScript），黑金配色 #000000 / #c8a45c。v5.0 要让所有前端数据从mock/static切换到真实API，确保产品可用。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5a-api-layer

【环境准备】
cd /tmp/mysticdao-frontend
git status
# 如果在 v3.1-agent-rewrite 分支:
git pull origin v3.1-agent-rewrite
git checkout -b agent-5a-api-layer

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → Ctrl+C → rm -rf node_modules package-lock.json → 再跑一次 → 还失败停止汇报
2. 每次修改后必须 npm run build，0 错误才能 git add
3. 基线代码已验证可 build，如果失败是你的修改导致的
4. 不要修改 hooks（useCamera.ts、useCompass.ts）
5. 不要修改相机/罗盘/3D组件

【Mira · 产品设计要求】
"用户看到'Loading...'超过3秒就会关闭页面。数据要来得快，错要处理得优雅。"

1. GlobalCounter接真实API（P0）
   • 文件: src/components/daily/GlobalCounter.tsx（修改）
   • 当前第9行有 `// TODO: Replace with real API`
   • 后端有计数API，端点格式: GET /api/stats/global
   • 返回: `{ totalDraws: number, todayDraws: number, activeUsers: number }`
   • 实现: 组件mount时fetch，失败则显示fallback静态数据（不报错）
   • 添加轮询: 每30秒刷新一次
   • 添加加载骨架屏（shimmer效果），避免数字跳动

2. API错误处理增强（P0）
   • 文件: src/services/api.ts（修改）
   • 所有API调用添加3次重试 + 指数退避（1s → 2s → 4s）
   • 新增统一错误提示: 网络错误 → "Connection unstable, retrying..."；服务端错误 → "Server busy, please try later"
   • fetchAIInterpretation已有AbortController，保持不变

3. 请求去重（P1）
   • 文件: src/services/api.ts 或新建 src/hooks/useApiCache.ts
   • 相同参数的请求在500ms内只发一次
   • 使用内存缓存 + localStorage持久化（5分钟过期）

【代码铁律】
1. 只改API层和GlobalCounter，不改业务逻辑
2. 保持现有TypeScript类型定义
3. 错误处理不能阻塞UI
4. 向后兼容: API不可用时静默降级到本地数据

【验收标准】
- npm run build 通过，0 TypeScript错误
- GlobalCounter显示真实数据（或优雅降级）
- 断网时页面不崩溃，显示友好提示
- 相同快速点击不重复请求

【额外要求】
- package.json version 改为 "5.0.0"
- 提交信息: `feat(api): real GlobalCounter + retry + dedup`

【推送】
git add -A
git commit -m "feat(api): real GlobalCounter + retry + dedup"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5a-api-layer

---

## 任务 2：Daily结果页 → 终极视觉 "灵签天启"

【项目背景】
MysticDao 每日运势页面结果展示已推倒重来2次，用户仍不满意"粗糙"。v5.0 需要一次终极重写，让结果页超越"网页"感，像"打开一幅上古灵签"。黑金配色 #000000 / #c8a45c。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5b-daily-result

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v3.1-agent-rewrite
git checkout -b agent-5b-daily-result

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → Ctrl+C → rm -rf node_modules → npm install
2. 每次修改后 npm run build，0 错误才能 git add
3. 基线代码已验证可 build
4. 不要修改 hooks（useCamera.ts、useCompass.ts）
5. 不要修改相机/罗盘/3D组件

【Mira · 产品设计要求】
"用户不会说'页面粗糙'，用户会直接关闭。结果页是唯一让用户觉得'这产品值'的地方。要有光、有动、有文化厚重感。"

1. 卷轴容器重构（P0）
   • 文件: src/pages/Daily.tsx（修改 result 部分，约300行）
   • 整个结果包裹在一个"古卷"中，卷轴上下有木质轴头（CSS渐变模拟木纹）
   • 卷轴从顶部向下展开: scaleY 0→1 动画，持续1.2s，ease [0.16, 1, 0.3, 1]
   • 卷轴纸张背景: 深褐色到黑色的径向渐变，带微弱噪点纹理
   • 卷轴边框: 1px金色渐变线，透明度0.1-0.3

2. 符咒视觉中心（P0）
   • TalismanRenderer 居中大幅展示，尺寸 340x510
   • 符咒周围有卦象颜色的径向光晕（blur-3xl, opacity 0.2-0.4），缓慢脉动
   • 符咒缓慢上下浮动动画: y [0, -8, 0], duration 5s, infinite
   • 符咒入场: 从下方浮现 + 透明度渐变，持续0.8s

3. 卦名书法效果（P0）
   • 卦名使用 `font-family: 'Noto Serif SC', Georgia, serif`
   • text-shadow: `0 0 40px ${color}50, 0 2px 8px rgba(0,0,0,0.6)`
   • 入场: 从模糊到清晰 + 放大，持续0.6s
   • 祈福主题文字: 金色半透明，字间距0.15em

4. 分数+印章仪式（P0）
   • RollingNumber: 数字从0滚动到目标值，持续1s，easeOut
   • 数字颜色随卦象变化，带发光text-shadow
   • 印章: 正方形边框，从放大+旋转弹入（scale 3→1, rotate -20→8）
   • 印章盖印时: 短暂金色闪光（opacity [0, 0.6, 0], 1.5s）
   • 印章等级: ≥85=上, ≥70=吉, ≥55=中, <55=平

5. 六爻绘制（P1）
   • 保留 HexagramDraw 组件，strokeColor随卦象颜色
   • 添加标签: 动爻=金色高亮，阳=实线，阴=断线

6. 运势维度（P1）
   • 4个 ScoreRing 横向排列
   • 依次入场动画，每个延迟150ms
   • 保留原有功能

7. AI解读古卷（P1）
   • 古卷样式容器: 圆角边框 + 金色渐变上下线
   • BreathingTypewriter: 保留逐字呼吸效果
   • 追问按钮: 朱砂红色调，圆角胶囊
   • 可折叠段落: 展开/收起动画

8. 操作按钮（P1）
   • 收藏/分享/再来一卦: 金色边框，hover时发光
   • 底部固定悬浮面板（移动端）

【代码铁律】
1. 只修改 Daily.tsx 的 result 部分（step === 'result'），不改动其他状态机逻辑
2. 保留所有现有功能: TalismanRenderer, HexagramDraw, BreathingTypewriter, 追问系统, ShareCard, SharePoster
3. 保留所有事件handler: handleFollowUp, handleSegmentComplete, handleReset 等
4. 不要修改数据结构和类型定义
5. 所有动画用 Framer Motion

【验收标准】
- npm run build 通过，0 TypeScript错误
- 卷轴展开动画流畅
- 符咒有光晕和浮动效果
- 印章盖印有仪式感
- 移动端完美适配（iPhone/Samsung/Pixel）
- 所有原有功能正常工作（追问、分享、收藏）

【额外要求】
- package.json version 改为 "5.0.0"
- 提交信息: `feat(daily): ultimate scroll result page with talisman glow and seal ritual`

【推送】
git add -A
git commit -m "feat(daily): ultimate scroll result page with talisman glow and seal ritual"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5b-daily-result

---

## 任务 3：性能优化 → 代码分割 + 懒加载 + 压缩

【项目背景】
MysticDao 当前 bundle 310KB gzip / 1080KB minified，接近500KB gzip限额。v5.0 需要做代码分割，降低首屏加载，提升Lighthouse性能评分。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5c-performance

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v3.1-agent-rewrite
git checkout -b agent-5c-performance

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 不要改业务逻辑，只改构建配置和加载方式

【Mira · 产品设计要求】
"用户不会等3秒。首屏必须在1秒内呈现。"

1. 代码分割（P0）
   • 文件: vite.config.ts（修改）
   • 配置 `rollupOptions.output.manualChunks`:
     - `vendor`: react, react-dom, framer-motion
     - `ui`: shadcn/ui 组件（从单个chunk拆出）
     - `three`: three, @react-three/fiber, @react-three/drei（风水3D页面专用）
     - `data`: src/data/ 下所有数据文件（gua64, hexagram-talismans, bagua等）
   • 目标: 首屏JS < 200KB gzip

2. 路由懒加载（P0）
   • 文件: src/App.tsx（修改）
   • 非首屏页面用 `React.lazy()` + `Suspense`:
     - Bazi, FengShui, CompassPage, UserProfile, Pricing 懒加载
     - Home 和 Daily 保持同步加载（核心路径）
   • Suspense fallback: 简单的太极旋转loading

3. 资源优化（P1）
   • 添加 vite-plugin-compression（gzip + brotli预压缩）
   • 字体预加载: index.html中添加 `<link rel="preload">`
   • 图片懒加载: 所有 `<img>` 添加 `loading="lazy"`

4. Bundle分析（P1）
   • 添加 `rollup-plugin-visualizer` 用于分析bundle构成
   • 输出 stats.html 到 dist/

【代码铁律】
1. 只改构建配置和路由加载方式
2. 不要改组件内部逻辑
3. 懒加载的fallback不能影响用户体验

【验收标准】
- npm run build 通过
- 首屏chunk < 200KB gzip
- Lighthouse Performance > 70
- 路由切换有loading状态
- 无构建警告

【额外要求】
- package.json version 改为 "5.0.0"
- 提交信息: `feat(perf): code splitting + lazy load + compression`

【推送】
git add -A
git commit -m "feat(perf): code splitting + lazy load + compression"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5c-performance

---

## 任务 4：移动端体验 → 响应式 + 触摸优化 + 相机适配

【项目背景】
MysticDao 在桌面端体验良好，但移动端（iPhone/Android）存在字体过小、按钮难点击、风水罗盘传感器兼容性等问题。v5.0 需要全面优化移动端体验。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5d-mobile

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v3.1-agent-rewrite
git checkout -b agent-5d-mobile

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误

【Mira · 产品设计要求】
"70%用户用手机。手机上体验不好 = 产品失败。"

1. Daily结果页移动端（P0）
   • 文件: src/pages/Daily.tsx（修改）
   • 字体最小14px（当前有些地方用text-[10px]）
   • 卷轴宽度在移动端改为 `max-w-[95vw]`
   • 按钮最小触摸区域 44x44pt
   • 底部添加固定悬浮操作栏（收藏/分享/再来一卦）
   • 防止键盘弹出时布局错乱

2. 风水罗盘传感器（P0）
   • 文件: src/components/compass/WebCompass.tsx（修改）
   • iOS Safari: 处理 `requestPermission()` 权限弹窗
   • Android Chrome: 兼容 `deviceorientationabsolute` 事件
   • 传感器不可用时优雅降级到手动输入方向
   • 添加方向校准提示（"请水平持握手机"）

3. 分享卡片适配（P1）
   • 文件: src/components/share/ShareCard.tsx（修改）
   • 小屏幕时: 平台选择器改为横向滚动
   • 导出图片尺寸自适应屏幕

4. 底部导航（P1）
   • 文件: src/components/Navbar.tsx（修改）
   • 移动端底部固定tab bar（Home/Daily/Bazi/FengShui/Profile）
   • 顶部Navbar在移动端隐藏或简化为logo

5. 新增响应式Hook（P1）
   • 文件: src/hooks/useMediaQuery.ts（新建）
   • 检测 isMobile / isTablet / isDesktop
   • 在组件中根据设备类型调整布局

【代码铁律】
1. 所有改动必须考虑桌面端不受影响
2. 触摸目标最小44x44pt
3. 字体最小14px
4. 不要修改业务逻辑

【验收标准】
- npm run build 通过
- iPhone 14 Pro / Samsung S23 / Pixel 7 三种尺寸无水平滚动
- 所有按钮可触摸
- 风水罗盘在真机上可获取方向
- 无布局错乱

【额外要求】
- package.json version 改为 "5.0.0"
- 提交信息: `feat(mobile): responsive daily + compass sensors + bottom nav`

【推送】
git add -A
git commit -m "feat(mobile): responsive daily + compass sensors + bottom nav"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5d-mobile

---

## 任务 5：测试体系 → Vitest + 单元测试 + CI

【项目背景】
MysticDao 目前无前端测试。v5.0 需要建立测试体系，覆盖核心工具函数和数据层，确保重构时不会破坏现有功能。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5e-testing

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v3.1-agent-rewrite
git checkout -b agent-5e-testing

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi
# 额外安装测试依赖
npm install -D vitest @vitest/ui jsdom @testing-library/react @testing-library/jest-dom

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build + npm run test，全通过才能 git add
3. 基线代码已验证可 build

【Mira · 产品设计要求】
"没有测试的代码是技术债务。v5.0要还清这笔债。"

1. 配置Vitest（P0）
   • 文件: vitest.config.ts（新建）
   • 环境: jsdom
   • 全局引入: @testing-library/jest-dom
   • 路径别名: 与vite.config.ts对齐（@/ → src/）

2. 工具函数测试（P0）
   • src/lib/theme.ts → theme.test.ts
     - 测试 getTheme('金')/getTheme('木')/... 返回正确颜色
     - 测试 getSealInfo(90)/getSealInfo(75)/... 返回正确等级
   • src/lib/cardRarity.ts → cardRarity.test.ts
     - 测试稀有度分配逻辑
   • src/lib/storage.ts → storage.test.ts
     - 测试 getAICachedUrl/setAICachedUrl/clearAICache
     - 使用 localStorage mock

3. 数据层测试（P1）
   • src/data/gua64.ts → gua64.test.ts
     - 验证64卦数据完整性（每卦有name/lower/upper/element）
   • src/data/hexagram-talismans.ts → hexagram-talismans.test.ts
     - 验证所有64卦都有映射

4. Hooks测试（P1）
   • src/hooks/useStreak.ts → useStreak.test.ts
     - 测试连续签到逻辑
     - 测试中断后重置

5. GitHub Actions CI（P1）
   • 文件: .github/workflows/ci.yml（新建）
   • 触发: push 到任意分支 + PR
   • 步骤: checkout → setup Node → npm install → npm run build → npm run test
   • 失败时阻止merge

【代码铁律】
1. 测试文件与源文件同目录，命名 `*.test.ts`
2. 不要修改被测试的源文件（除非发现bug）
3. 测试要独立，不依赖外部状态

【验收标准】
- npm run build 通过
- npm run test 全部通过
- 测试覆盖率 > 50%（核心模块）
- CI在GitHub上运行成功

【额外要求】
- package.json 添加 scripts: `"test": "vitest"`, `"test:ui": "vitest --ui"`
- package.json version 改为 "5.0.0"
- 提交信息: `feat(test): vitest + unit tests + CI pipeline`

【推送】
git add -A
git commit -m "feat(test): vitest + unit tests + CI pipeline"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5e-testing

---

## 任务 6：SEO & 无障碍 & PWA → 专业度提升

【项目背景】
MysticDao 目前缺少SEO优化、无障碍支持和PWA能力。v5.0 要让产品在搜索引擎可被发现、对残障用户友好、可添加到手机主屏。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v3.1-agent-rewrite
• 工作分支: agent-5f-seo-a11y-pwa

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v3.1-agent-rewrite
git checkout -b agent-5f-seo-a11y-pwa

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误

【Mira · 产品设计要求】
"如果Google搜不到我们，产品再好也没用。"

1. SEO优化（P0）
   • 文件: index.html（修改）
   • title: "MysticDao — Daily I Ching Divination & AI Wisdom"
   • meta description: "Discover your daily fortune with authentic I Ching hexagram casting. AI-powered readings, personalized talismans, and ancient Eastern wisdom."
   • Open Graph: og:title, og:description, og:image, og:url
   • Twitter Card: twitter:card, twitter:title, twitter:description
   • 添加 canonical link
   • 添加结构化数据 (JSON-LD): WebSite + WebApplication schema

2. 页面级SEO组件（P0）
   • 文件: src/components/SEO.tsx（新建）
   • 接收 props: title, description, image, url
   • 使用 react-helmet-async 管理 <head>
   • 每个页面调用: Home/Daily/Bazi/FengShui/Pricing/UserProfile 各有不同title

3. 无障碍（P1）
   • 所有 `<button>` 有 aria-label（无文字图标按钮必须）
   • 所有 `<img>` 有 alt 属性
   • 颜色对比度符合 WCAG AA（4.5:1文字，3:1大文字）
   • 键盘导航: Tab顺序合理，焦点可见
   • 减少动画偏好: 检测 `prefers-reduced-motion`，禁用非必要动画

4. PWA（P1）
   • 文件: public/manifest.json（新建）
   • name: "MysticDao", short_name: "MysticDao"
   • theme_color: "#000000", background_color: "#000000"
   • icons: 192x192 + 512x512（可用文字"☯"生成SVG转PNG）
   • display: "standalone"
   • Service Worker: public/sw.js（新建）
   • 缓存策略: Cache First for static assets
   • index.html 注册 sw: `<script>navigator.serviceWorker.register('/sw.js')</script>`

5. 性能（P1）
   • 字体预加载: `<link rel="preload" href="..." as="font" crossorigin>`
   • 关键CSS内联（如有必要）

【代码铁律】
1. 不要修改业务逻辑
2. SEO组件不能破坏现有渲染
3. PWA service worker不能缓存API请求（只缓存静态资源）

【验收标准】
- npm run build 通过
- Lighthouse SEO > 90
- Lighthouse Accessibility > 90
- PWA检测通过（Chrome DevTools → Application → Manifest + Service Workers）
- 每个页面有独特title

【额外要求】
- package.json version 改为 "5.0.0"
- 提交信息: `feat(seo): meta tags + a11y + PWA manifest + service worker`

【推送】
git add -A
git commit -m "feat(seo): meta tags + a11y + PWA manifest + service worker"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5f-seo-a11y-pwa

---

## 融合规则（给CLI Agent）

1. Agent输出代码后，先checkout到临时分支测试
2. Merge顺序: 5C(性能) → 5E(测试) → 5F(SEO) → 5A(API) → 5D(移动端) → 5B(Daily重写)
3. 5B最后merge，因为改动最大（Daily.tsx result部分重写）
4. 每次merge后: `npx tsc --noEmit` → `npx vite build` → 部署
5. 冲突解决: 功能逻辑 > 视觉效果 > 代码风格
6. 全部merge后打tag: `git tag v5.0.0 && git push origin v5.0.0`
