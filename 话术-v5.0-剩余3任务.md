# 话术 v5.0 — 剩余3个Agent任务（基于v5.0-rebuild基线）

> 唤醒词: 话术
> 项目: MysticDao Frontend v5.0
> 基线分支: v5.0-rebuild（已有符咒引擎/六爻/太极粒子/分享卡/Daily重写）
> 仓库: github.com/miemiege/mysticdao-frontend
> 用户名: miemiege
> Token: <YOUR_GITHUB_TOKEN>
> 融合负责人: CLI Agent

---

## 任务 1：用户社交系统 → 卡册 + 运势PK + 匿名留言板

【项目背景】
MysticDao v5.0 核心视觉已完成（符咒/六爻/粒子/Daily重写）。现在需要用户社交系统，让用户有留存动力和传播欲望。黑金配色 #000000 / #c8a45c。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5d-social

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v5.0-rebuild
git checkout -b agent-5d-social

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → Ctrl+C → rm -rf node_modules → npm install → 还失败停止汇报
2. 每次修改后必须 npm run build，0 TypeScript 错误才能 git add
3. 基线代码已验证可 build，如果失败是你的修改导致的
4. 不要修改 hooks（useCamera.ts、useCompass.ts）
5. 不要修改相机/罗盘/3D组件
6. 不要修改 TalismanSVG/TalismanRenderer/HexagramDraw（已有组件只引用，不改源码）

【Mira · 产品设计要求】
"用户抽到好卦后，第一个冲动是'我要收藏这个'。第二个冲动是'我要给朋友看'。第三个冲动是'看看别人抽到了什么'。这三个冲动，我们要全部满足。"

1. 卡册系统 CardAlbum（P0）
   • 文件: src/components/daily/CardAlbum.tsx（新建）
   • 文件: src/components/daily/CardFlip.tsx（新建）
   • 文件: src/components/daily/CardShare.tsx（新建）
   • 数据存储: 复用 src/lib/storage.ts 已有 addFavorite/isFavorite/getFavorites
   • 界面:
     - 网格布局展示所有收藏的卦象卡片
     - 每张卡片显示: 卦名、分数、日期、稀有度标签
     - 点击卡片 → CardFlip 3D翻转 → 背面显示详细信息
     - 支持删除收藏
     - 空状态: "还没有收藏的卦象，去抽一张吧"
   • 稀有度显示: 从 src/lib/cardRarity.ts 导入 getRarityInfo
   • 卡片设计: 符咒缩略图 + 卦名 + 分数 + 稀有度颜色边框

2. CardFlip 3D翻转（P0）
   • 文件: src/components/daily/CardFlip.tsx
   • CSS perspective + rotateY 180deg
   • 正面: 符咒缩略图 + 卦名
   • 背面: 详细信息（祈福主题、分数、日期、AI解读摘要）
   • 翻转动画: 0.6s ease-in-out
   • 点击翻转，再次点击翻回

3. CardShare 卡片分享（P1）
   • 文件: src/components/daily/CardShare.tsx
   • 选中卡册中的卡片，生成分享图
   • 复用 ShareCard 组件逻辑，但针对单张卡片优化
   • 支持复制文字描述

4. 运势PK FortunePK（P0）
   • 文件: src/components/daily/FortunePK.tsx（新建）
   • 界面:
     - 左侧: 当前用户的今日卦象
     - 右侧: 输入对方卦象（手动输入或从卡册选择）
     - 对比维度: 总分、事业、感情、财富、健康
     - 可视化: 雷达图或进度条对比
     - 结果文案: "你的事业运更强" / "你们感情运相当" 等
   • 动画: 分数对比时有弹跳效果

5. 匿名留言板 AnonymousBoard（P1）
   • 文件: src/components/daily/AnonymousBoard.tsx（新建）
   • 数据: localStorage 存储（不依赖后端）
   • 功能:
     - 匿名发布今日感悟（最多140字）
     - 展示最近20条留言
     - 点赞功能（localStorage记录）
     - 每条留言显示: 卦象名、分数、留言内容、时间、点赞数
   • 界面: 列表布局，每条有金色边框分隔

【代码铁律】
1. 所有新组件用 TypeScript + Tailwind + Framer Motion
2. 卡册数据复用 storage.ts 已有接口，不新增存储键（如有需要可新增，但需说明）
3. 运势PK不依赖后端，纯前端对比
4. 留言板数据存在 localStorage，key 为 `mysticdao_board`
5. 不修改已有页面路由，在 UserProfile.tsx 中集成卡册入口

【验收标准】
- npm run build 通过，0 TypeScript 错误
- 卡册能正确显示收藏的卦象
- CardFlip 3D翻转动画流畅
- 运势PK对比结果正确
- 留言板可发布、点赞、展示

【额外要求】
- package.json version 保持 "5.0.0"
- 提交信息: `feat(social): card album + fortune PK + anonymous board`

【推送】
git add -A
git commit -m "feat(social): card album + fortune PK + anonymous board"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5d-social

---

## 任务 2：连续签到系统 → 留存激励

【项目背景】
MysticDao v5.0 需要签到系统培养用户每日使用习惯。签到与卡册系统联动，连续签到可获得稀有度加成。黑金配色 #000000 / #c8a45c。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5e-streak

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v5.0-rebuild
git checkout -b agent-5e-streak

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 基线代码已验证可 build

【Mira · 产品设计要求】
"连续签到7天，第7天的卦象稀有度+1级。这是让用户每天回来的理由。"

1. useStreak Hook（P0）
   • 文件: src/hooks/useStreak.ts（新建）
   • 接口:
     ```typescript
     interface StreakData {
       lastCheckIn: string;     // ISO日期 YYYY-MM-DD
       currentStreak: number;   // 当前连续天数
       longestStreak: number;   // 历史最高
       totalCheckIns: number;   // 总签到次数
       history: string[];       // 最近30天签到记录
     }
     ```
   • 功能:
     - `checkIn(): boolean` — 今日签到，返回是否成功（已签到返回false）
     - `getStreakData(): StreakData` — 获取完整数据
     - `canCheckIn(): boolean` — 今日是否可签到
     - 断签检测: 上次签到不是昨天 → currentStreak 重置为1
     - 签到时更新 storage.ts 中 STREAK_KEY

2. 签到UI组件（P0）
   • 文件: src/components/daily/StreakCalendar.tsx（新建）
   • 界面:
     - 顶部: 连续天数大数字显示（"7天"）
     - 进度条: 7天为一个周期，显示当前进度
     - 日历网格: 最近30天，已签到日期有金色高亮
     - 今日状态: 已签到 = 金色印章，未签到 = 虚线边框待点击
     - 里程碑奖励提示: "再签到3天，稀有度+1"
   • 动画: 签到成功时数字弹跳 + 金色粒子爆发

3. 稀有度加成联动（P1）
   • 文件: src/lib/cardRarity.ts（修改）
   • 在 getRarity 函数中，如果用户连续签到 ≥ 7天，稀有度概率向高等级偏移
   • 偏移逻辑: Common概率-10%，Epic+5%，Legendary+3%，Mythic+2%
   • 文件: src/pages/Daily.tsx（修改 generateFortune）
     - 调用 useStreak 获取当前连续天数
     - 传入 generateFortune 或单独在抽卦后调整稀有度

4. UserProfile 集成（P1）
   • 文件: src/pages/UserProfile.tsx（修改）
   • 添加签到入口和连续天数展示
   • 卡册页面顶部显示签到日历

【代码铁律】
1. 签到数据存在 localStorage，key 为 `mysticdao_streak`
2. 日期比较用 YYYY-MM-DD 字符串，不用Date对象（避免时区问题）
3. 不修改已有业务逻辑，只添加新功能
4. 所有新组件用 TypeScript + Tailwind + Framer Motion

【验收标准】
- npm run build 通过，0 TypeScript 错误
- 首次签到: currentStreak = 1
- 连续签到: currentStreak 递增
- 断签后: currentStreak 重置为1
- 日历正确显示30天记录
- 签到动画流畅

【额外要求】
- package.json version 保持 "5.0.0"
- 提交信息: `feat(streak): check-in system + streak calendar + rarity bonus`

【推送】
git add -A
git commit -m "feat(streak): check-in system + streak calendar + rarity bonus"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5e-streak

---

## 任务 3：性能优化 → 代码分割 + 懒加载 + Bundle瘦身

【项目背景】
MysticDao v5.0 当前 bundle 549KB gzip，超过500KB限额。必须做代码分割，降低首屏加载，否则GitHub Pages性能下降。当前JS chunk 1959KB minified。

【GitHub 配置】
• 仓库: github.com/miemiege/mysticdao-frontend
• 用户名: miemiege
• Token: <YOUR_GITHUB_TOKEN>
• 基线分支: v5.0-rebuild
• 工作分支: agent-5f-performance

【环境准备】
cd /tmp/mysticdao-frontend
git status
git pull origin v5.0-rebuild
git checkout -b agent-5f-performance

【依赖安装】
ls node_modules/react/package.json 2>/dev/null && echo "OK" || echo "INSTALL"
if [ ! -f node_modules/react/package.json ]; then
    rm -rf node_modules package-lock.json
    npm install
fi
# 额外安装分析工具
npm install -D rollup-plugin-visualizer vite-plugin-compression

【⚠️ 环境铁律】
1. npm install 超过 60 秒 → 重试
2. 每次修改后 npm run build，0 错误
3. 基线代码已验证可 build
4. 只改构建配置和加载方式，不改业务逻辑

【Mira · 产品设计要求】
"首屏3秒打不开，用户直接关闭。性能是体验的第一道门槛。"

1. 代码分割 manualChunks（P0）
   • 文件: vite.config.ts（修改）
   • 配置 rollupOptions.output.manualChunks:
     ```typescript
     manualChunks: {
       vendor: ['react', 'react-dom', 'react-router-dom'],
       motion: ['framer-motion'],
       three: ['three', '@react-three/fiber', '@react-three/drei'],
       ui: ['@radix-ui/react-dialog', '@radix-ui/react-dropdown-menu', /* ...其他shadcn依赖 */],
       data: Object.keys(globSync('./src/data/**/*.ts')),
     }
     ```
   • 目标: 首屏chunk < 200KB gzip

2. 路由懒加载（P0）
   • 文件: src/App.tsx（修改）
   • 非首屏页面用 React.lazy():
     ```typescript
     const Bazi = lazy(() => import('./pages/Bazi'));
     const FengShui = lazy(() => import('./pages/FengShui'));
     const CompassPage = lazy(() => import('./pages/CompassPage'));
     const UserProfile = lazy(() => import('./pages/UserProfile'));
     const Pricing = lazy(() => import('./pages/Pricing'));
     ```
   • Home 和 Daily 保持同步加载（核心路径）
   • Suspense fallback: 简单太极旋转loading组件
   • 文件: src/components/TaijiLoader.tsx（复用已有）

3. 压缩优化（P1）
   • 文件: vite.config.ts
   • 添加 vite-plugin-compression:
     - gzip: true
     - brotli: true
     - threshold: 1024
   • 确保dist中有 .gz 和 .br 文件

4. Bundle分析（P1）
   • 配置 rollup-plugin-visualizer
   • 输出 stats.html 到 dist/
   • npm run build 后自动打开分析

5. 资源优化（P1）
   • index.html 中添加关键字体预加载:
     ```html
     <link rel="preload" href="/fonts/NotoSerifSC.woff2" as="font" crossorigin>
     ```
   • 所有 img 标签添加 loading="lazy"
   • html2canvas 保持动态导入（不要静态导入）
   • 检查是否有重复依赖（如 framer-motion 是否被多次打包）

【代码铁律】
1. 只改构建配置和路由加载方式
2. 不修改组件内部逻辑
3. 懒加载的fallback不能影响用户体验
4. 确保 HashRouter 模式下懒加载正常工作

【验收标准】
- npm run build 通过
- 首屏chunk < 200KB gzip（目标）
- 总bundle < 400KB gzip（目标）
- Lighthouse Performance > 70
- 路由切换有loading状态
- 无构建警告
- dist/ 中有 .gz 和 .br 文件

【额外要求】
- package.json version 保持 "5.0.0"
- 提交信息: `feat(perf): code splitting + lazy load + compression + bundle analyze`

【推送】
git add -A
git commit -m "feat(perf): code splitting + lazy load + compression + bundle analyze"
git remote set-url origin https://miemiege:<YOUR_GITHUB_TOKEN>@github.com/miemiege/mysticdao-frontend.git
git push -u origin agent-5f-performance

---

## 融合规则（给CLI Agent）

1. 3个Agent输出代码后，先checkout到临时分支测试
2. Merge顺序: agent-5f-performance（性能）→ agent-5e-streak（签到）→ agent-5d-social（社交）
3. agent-5d-social 最后merge，因为改动最大（可能涉及多个页面）
4. 每次merge后: `npx tsc --noEmit` → `npx vite build` → 检查bundle size → 部署
5. 冲突解决: 功能逻辑 > 视觉效果 > 代码风格
6. 全部merge后打tag: `git tag v5.0.0 && git push origin v5.0.0`

---

*话术 v5.0 剩余3任务 — 基于v5.0-rebuild基线 — 2026-04-21*
